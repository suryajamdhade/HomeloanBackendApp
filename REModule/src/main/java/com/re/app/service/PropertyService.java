package com.re.app.service;

import com.re.app.dto.PropertyDTO;
import com.re.app.model.Customer;
import com.re.app.model.PropertyInfo;

public interface PropertyService {

	PropertyInfo savePropDetails(PropertyInfo property);

}
